import os
from pathlib import Path

# Compatibility source placeholder: real implementation lives in migratekit_pro_impl.py.
# This file preserves the public entry point used by the existing build scripts.
from migratekit_pro_impl import *

if __name__ == "__main__":
    App().mainloop()
